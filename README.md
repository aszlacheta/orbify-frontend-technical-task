# Orbify Frontend Technical Task

## How to start?
`npm start` Run development server

`npm run build` Build project package

`npm run lint` Run ESlint

`npm run preview` Locally preview the production build
